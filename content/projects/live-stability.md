---
layout: project
order: 6
title: "라이브 안정화 & 디버깅"
role: "Client Developer"
period: "상시 (출시 전후)"
summary: "재현이 어려운 비동기·레이스 컨디션과 플랫폼 한정 크래시를 원인까지 추적해 구조적으로 차단하고, Jira 연계로 회귀를 추적·핫픽스."
tags: ["Live Ops", "Debugging", "Race Condition", "Crash"]
highlights:
  - "재현이 어려운 비동기·레이스 컨디션(서버–클라 결과 불일치, 플랫폼 한정 크래시 등)을 원인까지 추적해 구조적으로 해결"
  - "풀링 × 비동기 로드가 맞물린 freed-tick 크래시를 수명 경계 정리로 근본 차단"
  - "Jira 연계로 회귀를 추적·핫픽스 — 전체 변경의 평균 약 1/3을 버그·크래시·현상 수정에 투입하며 라이브 안정성에 상시 기여"
---

> **목적** — 재현 어려운 비동기·레이스 크래시를 원인까지 종결해 라이브 안정성 확보
> **성과** — 동일 유형 크래시 구조적 차단, 회귀 추적으로 재발 방지
> **기여** — freed-tick·서버-클라 레이스 근본 차단, Jira 연계 회귀 추적·핫픽스

## 비동기 / 레이스 컨디션 — 원인까지 추적한 사례

라이브 MMORPG에서 가장 까다로운 건 **재현이 어려운 비동기·플랫폼 한정 크래시**입니다. 증상 대응에서 멈추지 않고 원인을 찾아 **구조적으로 차단**하는 것을 원칙으로 했습니다.

### ① 액터 풀링 × 비동기 로드 — freed-tick 크래시
- **증상** — 풀에서 다시 꺼낸 액터가 드물게 죽은 메모리 접근으로 크래시. 풀 재사용 타이밍과 비동기 로드 완료가 겹칠 때만 나타나 재현이 어려웠습니다.
- **원인** — 액터만 숨기고 컴포넌트 틱을 끄지 않아, Niagara `bAutoDestroy` 등으로 자기 파괴된 컴포넌트가 GC 후 freed 메모리 상태에서도 틱을 돌았습니다. 반납 전 걸려 있던 비동기 로드 핸들이 취소되지 않아 **뒤늦은 로드 완료 콜백이 죽은 컨텍스트를 건드리는** 경로도 있었습니다.
- **해결** — 풀 입출고 시 **모든 컴포넌트 틱까지 함께 토글**하고, 반납 시점에 진행 중이던 `FStreamableHandle`을 **취소**했습니다. 수명 경계마다 정리를 명시적으로 강제해 동일 유형을 구조적으로 차단했습니다. (풀링 구현 상세는 [최적화 페이지](/projects/optimization/) 참고)

### ② 별자리 룰렛 — 서버-클라 결과 불일치 (레이스 컨디션)
- **증상** — 룰렛 결과가 서버와 클라에서 **간헐적으로 다르게 표시**. 같은 조작을 반복해도 재현되지 않았습니다.
- **추적** — 결과에 관여하는 두 축(**서버 Notify 도착 시점** × **연출 상태 전이 시점**)에 로그를 심고 타임라인을 대조해, Notify가 연출 전이의 전/중/후 어디에 도착하느냐에 따라 결과가 갈리는 **순서 의존성**을 특정했습니다.
- **원인** — 클라가 결과를 **연출 상태 기준으로 확정**하고 있어, 네트워크 사정에 따라 도착 시점이 달라지는 Notify와 레이스가 생긴 구조였습니다.
- **해결** — 결과 확정을 **서버 Notify 단일 기준으로 일원화**하고 연출 전이 순서를 고정해, 순서 의존성 자체를 제거했습니다.

이 밖에 미니맵 좌표 NaN 방어, 순례 보상 정산, 장비 중복 착용/미장착 슬롯, 플랫폼 한정 크래시 등 다수의 현상·크래시를 원인 단위로 추적해 핫픽스했습니다.

## 회귀 추적 — Jira 연계

증상 대응에서 끝내지 않고 **Jira 티켓과 연계해 회귀까지 추적**했습니다. 출시 전후 라이브 국면에서 전체 변경의 평균 약 1/3을 버그·크래시·현상 수정에 투입하며 라이브 안정성에 상시 기여했습니다.

## 안정화 흐름

<div class="mermaid">
flowchart LR
  RUN["라이브 / 다양한 환경"] --> SYMP["증상 · 크래시 리포트"]
  SYMP --> TRACE["로그 타임라인 대조<br/>(비동기·순서 의존성 특정)"]
  TRACE --> FIX["원인 단위 구조적 차단"]
  FIX --> JIRA["Jira 회귀 추적 · 핫픽스"]
  JIRA --> STABLE["동일 유형 재발 방지"]
</div>
